if(prev['직급명'] !== row['직급명']) changes.push({type:'직급변동', before:displayTitleChange(row['성명'], prev['직급명'], 'before'), after:displayTitleChange(row['성명'], row['직급명'], 'after'), tag:'promo', position:displayTitleChange(row['성명'], row['직급명'], 'after')});
